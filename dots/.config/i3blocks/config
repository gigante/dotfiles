# i3blocks config file
# List of valid properties:
# align, color, command, full_text, instance, interval, label, min_width
# name, separator, separator_block_width, short_text, signal, urgent

# Global properties
command=~/bin/blocklets/$BLOCK_NAME
separator_block_width=10
markup=none

[mediaplayer]
label=
instance=spotify
interval=5
signal=10

[memory]
label=
instance=MEM
markup=pango
interval=30

[cpu_usage]
label=
markup=pango
interval=10

[disk]
label=
markup=pango
interval=30

[volume]
label=
instance=Master
interval=once
signal=10

[battery]
markup=pango
interval=30

[time]
label=
command=date '+%d/%m %H:%M'
interval=1
